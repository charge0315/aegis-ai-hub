# Aegis Nexus ユーザーマニュアル

Aegis Nexus（aegis-ai-hub）は、Gemini 3.1 AI と RSS フィードを統合した、自律学習型のインテリジェンス・ダッシュボードです。このマニュアルでは、各画面の機能と活用方法を解説します。

---

## 1. メインダッシュボード (Intelligence Feed)

メイン画面では、収集・解析された最新の記事がカテゴリごとに表示されます。

- **記事カードの閲覧**:
    - 各カードには記事のタイトル、概要、抽出された画像が表示されます。
    - **AI Reasoning**: AI がその記事を推奨する理由（なぜあなたの興味に合致するのか）が「Reasoning」として表示されます。
    - **スコアリング**: 記事の関連度が数値で表示されます。ブランド一致（+10点）、キーワード一致（+8点）などで算出されます。
- **日本語フィルタリング**:
    - ヘッダーにある「JA Only」トグルをオンにすると、日本語の記事のみが表示されます。
    - デフォルトでは、日本語記事が優先的に上位に並ぶようソートされています。
- **手動更新 (Refresh)**:
    - 右上の更新ボタンをクリックすると、すべての登録フィードから最新の記事を再取得します。
- **表示モードの切り替え**:
    - 表示サイズを「Small / Medium / Large」から選択できます。
    - 画像の表示 / 非表示を切り替えて、情報密度を調整可能です。

---

## 2. Nexus Editor (Command & Control)

「Nexus Command」からアクセスできる、システムの心臓部です。

- **カテゴリの編集**:
    - サイドバーからカテゴリを選択し、名前の変更、絵文字の変更、削除、並び替え（ドラッグ＆ドロップ）が行えます。
- **キーワード / ブランドの管理**:
    - **Target Brands**: あなたが注目している企業や製品名。
    - **Signal Keywords**: 収集のトリガーとなる重要なキーワード。
    - これらは手動で追加・削除できるほか、**「AI Suggest」**ボタンを押すことで、Gemini がカテゴリ名に基づいた最適な候補を提案します。
- **AI Restructure (全面刷新)**:
    - ヘッダーの「AI Restructure」ボタンを使用すると、AI が既存の全データを分析し、現在のトレンドに合わせて**最適な 10 カテゴリにプロフィールを再構築**します。
    - 既存の購読フィードも新しいカテゴリへ自動的に再割り当てされ、さらに高品質な新しい情報源も自動で追加されます。

---

## 3. Knowledge Graph

あなたの興味関心のネットワークを視覚化したマップです。

- **可視化の見方**:
    - 中心にあるカテゴリから、関連するブランドやキーワードがネットワーク状に広がっています。
    - ノード（円）をクリックすることで、個別のキーワードの有効/無効を切り替えるなど、直感的な操作が可能です。

---

## 4. Skill Registry

自律エージェントたちの能力を管理します。

- **エージェントの役割**:
    - **Archivist**: 記事の収集とトレンドの記憶、学習を担当。
    - **Curator**: 記事のスコアリングと推奨理由の生成を担当。
    - **Discovery**: 新しい情報源（RSSフィード）の探索を担当。
    - **Architect**: システム全体の構造とセマンティックフィルタリングを管理。
- **スキルの管理**:
    - 「RSS取得」「エンティティ抽出」「推論エンジン」などのスキルを個別に有効 / 無効化できます。

---

## 5. AI Insights

AI が自律的に学習した、新しいトレンドキーワードを管理する場所です。

- **Discover Trends Now**:
    - このボタンを押すと、`Archivist` エージェントが現在のフィードを深く分析し、あなたの現在の設定には含まれていないが重要度の高い「兆し（シグナル）」を発見します。
- **発見されたトレンドの処理**:
    - **Promote to Keyword**: 発見された単語を承認し、正式なキーワードとしてカテゴリに追加します。これにより、以降の収集精度が向上します。
    - **Dismiss**: 不要なキーワードを却下し、リストから削除します。

---

## 6. System Settings

システムの基本動作を設定します。

- **Gemini API Key**:
    - Google Gemini 3.1 を利用するための API キーを設定します。キーがない場合、AI による要約、提案、トレンド探索などの機能が制限されます。
- **Restore Default Profile (Factory Reset)**:
    - すべてのカテゴリ設定とフィード設定を初期状態に戻します（APIキーは保持されます）。
- **自動更新設定**:
    - バックグラウンドでの記事更新間隔などを設定可能です。

---

© 2026 Aegis AI Hub Team.
